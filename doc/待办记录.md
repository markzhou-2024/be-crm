待办工作：

1）admin用户可以全局统计，非admin用户以user_id进行统计，只统计自己名下的门店，以及门店所属客户数据。
在“我的页面”通过“门店与顾客管理”进入admin管理页面，需要admin通过账号密码登录进入，

2）需要做一个“月度经营指标统计”页面，主要有指标：
默认该顾问（user_id）下所以门店（shops数据集中按照user_id字段进行汇总）和顾客记录汇总信息（customers数据集中按照user_id字段进行汇总），可选择单个门店进行统计：
总门店门店数量（在顶部右上角显示）

一、门店数据：
1、到客数量-通过顾客消耗（consume集合）进行计算（consume数据集中user_id为登录用户的纳入数据统计，当天有同一个customer_id用多条记录的，按照一次到店统计，消耗时间在统计月份纳入统计。）
2、老客到店量（不含新客户）
3、新客数量（新客户定义，在客户购买商品页面上有“新客户首次购买”的选项，如果选择了，同步为顾问打上一个“新客户”标签，该标签在下个月1号0点起失效）
4、老客本月到店次数，统计老客活跃度。

二、服务数据：
顾问到店次数（数据来源bookings数据集，根据选择的时间start_ts和门店维度store_id，并且需要满足arrived字段为true时才进行统计，有一条数据就是到店一次）
顾问服务次数
门店自主服务次数

三、财务数据

1、本月销售额（通过buy数据集中的amount字段进行汇总统计）
1、本月的消耗金额。消耗金额公式：套盒总价/服务次数*本月的消耗次数 

3）客户首页优化（增加帅选，去除“全部门店的按钮”）--完成

4）做一个在售产品的维护页面，做好后支持在顾客购买页中选择（目前是手工写入），或者还有一种思路，在顾客下单时，人工输入的产品名称可以自动保存到数据库。这两种情况哪个更适合